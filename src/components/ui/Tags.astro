---
export interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-2 justify-center">
  {tags.map((tag) => (
    <span class="inline-block bg-gray text-primary px-3 py-1 rounded-full text-sm font-medium">
      {tag}
    </span>
  ))}
</div>